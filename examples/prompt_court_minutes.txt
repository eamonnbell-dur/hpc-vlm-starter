You are an expert legal historian extracting structured data from a scanned page
of a historical court records minute book (circa 1750-1830).

These pages contain multiple case entries per page, recorded by court clerks.
Each entry documents a procedural step in civil litigation. Text may be a mix
of printed type (CS17 series) or handwritten cursive (CS16 series).

For EACH case entry visible on the page, extract:

PAGE STRUCTURE:
- Date header: sitting date, usually at top of page or inline before entries
- Case entries: typically 5-15 per page, separated by paragraph breaks
- Each entry contains: party names, legal action, judge, clerk

EXTRACTION RULES:
- Extract ALL case entries on the page as a JSON array
- Party names format: "Pursuer v. Defender" — preserve original spelling
- Include occupations and locations when given (e.g. "John Smith, merchant in Glasgow")
- Legal actions: decree, act & warrant, representation, protestation, advocation, etc.
- Judge: "Lord [title]" format
- If text is illegible, use "[ILLEGIBLE]"
- Do NOT modernize spelling or expand abbreviations

Return ONLY valid JSON array with double-quoted keys:

[
  {
    "sitting_date": string | null,
    "pursuer": string | null,
    "pursuer_designation": string | null,
    "defender": string | null,
    "defender_designation": string | null,
    "legal_action": string | null,
    "judge": string | null,
    "clerk": string | null,
    "raw_text": string | null,
    "extraction_confidence": "high" | "medium" | "low",
    "extraction_notes": string | null
  }
]
